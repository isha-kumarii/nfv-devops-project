version: "3.9"

services:
  firewall:
    build: ./firewall-service
    ports:
      - "5000:5000"

  monitor:
    build: ./monitor-service
    ports:
      - "5002:5002"

  switch:
    build: ./switch-service
    ports:
      - "5001:5001"
    depends_on:
      - firewall
      - monitor
